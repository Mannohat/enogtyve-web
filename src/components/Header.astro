---
interface Props {
    lang?: "da" | "en";
}

const { lang = "da" } = Astro.props;
// TODO: When English pages are built, remove navLang and use `lang` instead in menuItems and exclusiveMenuItem
const navLang = "da";
const currentPath = Astro.url.pathname;

const menuItems = [
    {
        label: "Velkommen",
        href: `/${navLang}`,
        submenu: [
            {
                label: "Lidt om penge",
                href: `/${navLang}/blog/penges-historie`,
            },
            {
                label: "Lidt om Bitcoin",
                href: `/${navLang}/blog/hvad-er-bitcoin`,
            },
            {
                label: "White Paper",
                href: `/pdf/bitcoin.pdf`,
                target: "_blank",
            },
            {
                label: "Cypherpunk's Manifesto",
                href: `/pdf/cypherpunks-manifesto.pdf`,
                target: "_blank",
            },
            { label: "FUD / Fakta", href: `/${navLang}/velkommen/fud-facts` },
        ],
    },
    {
        label: "Kom igang",
        href: `/${navLang}/kom-igang`,
        submenu: [
            {
                label: "Anskaffelse af bitcoin",
                href: `/${navLang}/kom-igang/kob-bitcoin`,
            },
            {
                label: "Optjen bitcoin",
                href: `/${navLang}/kom-igang/optjen-bitcoin`,
            },
            {
                label: "Opbevaring af bitcoin",
                href: `/${navLang}/kom-igang/opbevaring-af-bitcoin`,
            },
            {
                label: "Hardware Wallets",
                href: `/${navLang}/kom-igang/hardware-wallets`,
            },
            {
                label: "Gendannelsesord / Seed Phrase",
                href: `/${navLang}/kom-igang/seed-phrase`,
            },
            {
                label: "Bitcoin Software",
                href: `/${navLang}/kom-igang/bitcoin-software`,
            },
            {
                label: "Privatliv og best Practice",
                href: `/${navLang}/kom-igang/privatliv-best-practice`,
            },
            {
                label: "Begreber og Ord",
                href: `/${navLang}/kom-igang/begreber-ord`,
            },
            {
                label: "Bitcoin Bøger på Dansk",
                href: `/${navLang}/kom-igang/bitcoin-boeger-dansk`,
            },
        ],
    },
    {
        label: "Lær mere",
        href: `/${navLang}/laer-mere`,
        submenu: [
            {
                label: "Bitcoin Blokkæden",
                href: `/${navLang}/laer-mere/bitcoin-blockchain`,
            },
            {
                label: "Bitcoin Lightning",
                href: `/${navLang}/laer-mere/lightning`,
            },
            {
                label: "Lightning Wallets",
                href: `/${navLang}/laer-mere/lightning-wallets`,
            },
            { label: "Liquid", href: `/${navLang}/laer-mere/liquid` },
            { label: "eCash", href: `/${navLang}/laer-mere/ecash` },
            {
                label: "Quotes oversat til Dansk",
                href: `/${navLang}/laer-mere/quotes-dansk`,
            },
            {
                label: "Bitcoin og din forretning",
                href: `/${navLang}/laer-mere/bitcoin-og-din-forretning`,
            },
            {
                label: "Lightning Produkter og Services",
                href: `/${navLang}/laer-mere/lightning-produkter-services`,
            },
            {
                label: "Bitcoin Services og Produkter",
                href: `/${navLang}/laer-mere/bitcoin-services-og-produkter`,
            },
            {
                label: "Value 4 Value",
                href: `/${navLang}/laer-mere/value4value`,
            },
        ],
    },
    { label: "Podcasts", href: `/${navLang}/podcasts`, submenu: [] },
    { label: "Blog", href: `/${navLang}/blog`, submenu: [] },
    {
        label: "Mining",
        href: `/${navLang}/mining`,
        submenu: [
            { label: "Solo Mining", href: `/${navLang}/mining/solo-mining` },
            {
                label: "Mining for opvarmning",
                href: `/${navLang}/mining/mining-for-opvarmning`,
            },
        ],
    },
    {
        label: "Udforsk",
        href: `/${navLang}/udforsk/udforsk`,
        submenu: [
            {
                label: "Mød ligesindede når du rejser",
                href: `/${navLang}/udforsk/moed-andre`,
            },
            {
                label: "Bitcoin Fællesskaber og Meetups i verden",
                href: `/${navLang}/udforsk/in-the-world`,
            },
            {
                label: "Bitcoin Cirkulære Økonomier",
                href: `/${navLang}/udforsk/cirkulaere-okonomier`,
            },
            {
                label: "Frie Byer og Samfund",
                href: `/${navLang}/udforsk/freecities`,
            },
            {
                label: "Bitcoin Konferencer",
                href: `/${navLang}/udforsk/konferencer`,
            },
            { label: "Nyheder", href: `/${navLang}/udforsk/nyheder` },
            {
                label: "Bitcoin Ressourcer",
                href: `/${navLang}/udforsk/bitcoin-ressources`,
            },
            {
                label: "Twentyone.world",
                href: `/${navLang}/udforsk/twentyoneworld`,
            },
        ],
    },
    {
        label: "Idé Værksted",
        href: `/${navLang}/idevaerksted/buidl`,
        submenu: [{ label: "Buidl", href: `/${navLang}/idevaerksted/buidl` }],
    },
    {
        label: "Fællesskab",
        href: `/${navLang}/faellesskab`,
        submenu: [
            { label: "Bogmærker", href: `/${navLang}/faellesskab/bookmarks` },
            {
                label: "Bitcoin Initiativer i Danmark",
                href: `/${navLang}/faellesskab/initiativer-danmark`,
            },
            { label: "Support", href: `/${navLang}/faellesskab/support` },
            { label: "Medie Kit", href: `/${navLang}/faellesskab/mediekit` },
            {
                label: "Spotify Playliste",
                href: `/${navLang}/faellesskab/playliste`,
            },
            { label: "Rabatter", href: `/${navLang}/rabat` },
        ],
    },
];

const exclusiveMenuItem = {
    label: "Rådgivning & Workshops",
    href: `/${navLang}/advisory-services/raadgivning`,
    submenu: [],
};

// Helper function to check if a path is active
function isActive(href: string, currentPath: string): boolean {
    if (href === `/${navLang}` || href === `/${navLang}/`) {
        return currentPath === `/${navLang}` || currentPath === `/${navLang}/`;
    }
    return currentPath.startsWith(href);
}

// Helper function to get language-switched path
function getSwitchedPath(targetLang: string): string {
    const pathWithoutLang = currentPath.replace(/^\/(da|en)/, "");
    return `/${targetLang}${pathWithoutLang || ""}`;
}
---

<header>
    <div class="header-top">
        <div class="container">
            <a href={`/${navLang}`} class="logo" aria-label="Home">
                <img src="/images/logos/enogtyve_logo.png" alt="Enogtyve" />
            </a>

            <div class="header-right">
                <button class="support-btn" onclick="openSupport()">
                    <span class="heart">♥</span>
                    <span>Støt med sats ⚡</span>
                </button>

                <div class="language-switcher">
                    <a
                        href={getSwitchedPath("da")}
                        class={lang === "da" ? "active" : ""}
                        aria-label="Switch to Danish"
                        aria-current={lang === "da" ? "true" : undefined}
                    >
                        DA
                    </a>
                    <span>/</span>
                    <a
                        href="/en/"
                        class={lang === "en" ? "active" : ""}
                        aria-label="Switch to English"
                        aria-current={lang === "en" ? "true" : undefined}
                    >
                        EN
                    </a>
                </div>

                <button
                    class="mobile-menu-toggle"
                    aria-label="Toggle mobile menu"
                    aria-expanded="false"
                    aria-controls="main-navigation"
                >
                    <span class="hamburger"></span>
                </button>
            </div>
        </div>
    </div>

    <nav class="nav-bar" id="main-navigation" aria-label="Main navigation">
        <div class="container nav-container">
            <ul class="nav-links" role="menubar">
                {
                    menuItems.map((item, index) => (
                        <li
                            class={item.submenu.length > 0 ? "has-submenu" : ""}
                            role="none"
                        >
                            {item.submenu.length > 0 ? (
                                <button
                                    class={`nav-button ${isActive(item.href, currentPath) ? "active" : ""}`}
                                    role="menuitem"
                                    aria-haspopup="true"
                                    aria-expanded="false"
                                    aria-label={`${item.label} category`}
                                >
                                    {item.label}
                                </button>
                            ) : (
                                <a
                                    href={item.href}
                                    class={
                                        isActive(item.href, currentPath)
                                            ? "active"
                                            : ""
                                    }
                                    role="menuitem"
                                >
                                    {item.label}
                                </a>
                            )}
                            {item.submenu.length > 0 && (
                                <ul
                                    class="submenu"
                                    role="menu"
                                    aria-label={`${item.label} submenu`}
                                >
                                    {item.submenu.map((subItem) => (
                                        <li role="none">
                                            <a
                                                href={subItem.href}
                                                class={
                                                    isActive(
                                                        subItem.href,
                                                        currentPath,
                                                    )
                                                        ? "active"
                                                        : ""
                                                }
                                                target={subItem.target}
                                                rel={
                                                    subItem.target === "_blank"
                                                        ? "noopener noreferrer"
                                                        : undefined
                                                }
                                                role="menuitem"
                                            >
                                                {subItem.label}
                                            </a>
                                        </li>
                                    ))}
                                </ul>
                            )}
                        </li>
                    ))
                }
            </ul>

            <ul class="nav-links exclusive-nav" role="menubar">
                <li
                    class={`exclusive ${exclusiveMenuItem.submenu.length > 0 ? "has-submenu" : ""}`}
                    role="none"
                >
                    {
                        exclusiveMenuItem.submenu.length > 0 ? (
                            <button
                                class={`nav-button ${isActive(exclusiveMenuItem.href, currentPath) ? "active" : ""}`}
                                role="menuitem"
                                aria-haspopup="true"
                                aria-expanded="false"
                                aria-label={`${exclusiveMenuItem.label} category`}
                            >
                                {exclusiveMenuItem.label}
                            </button>
                        ) : (
                            <a
                                href={exclusiveMenuItem.href}
                                class={
                                    isActive(
                                        exclusiveMenuItem.href,
                                        currentPath,
                                    )
                                        ? "active"
                                        : ""
                                }
                                role="menuitem"
                            >
                                {exclusiveMenuItem.label}
                            </a>
                        )
                    }
                    {
                        exclusiveMenuItem.submenu.length > 0 && (
                            <ul
                                class="submenu"
                                role="menu"
                                aria-label={`${exclusiveMenuItem.label} submenu`}
                            >
                                {exclusiveMenuItem.submenu.map((subItem) => (
                                    <li role="none">
                                        <a
                                            href={subItem.href}
                                            class={
                                                isActive(
                                                    subItem.href,
                                                    currentPath,
                                                )
                                                    ? "active"
                                                    : ""
                                            }
                                            target={subItem.target}
                                            rel={
                                                subItem.target === "_blank"
                                                    ? "noopener noreferrer"
                                                    : undefined
                                            }
                                            role="menuitem"
                                        >
                                            {subItem.label}
                                        </a>
                                    </li>
                                ))}
                            </ul>
                        )
                    }
                </li>
            </ul>
        </div>
    </nav>
</header>

<style>
    header {
        background: var(--background-alt);
        border-bottom: 2px solid var(--primary);
        position: relative;
    }

    .header-top {
        padding: 2rem 0 1.5rem;
    }

    .header-top .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .header-right {
        display: flex;
        align-items: center;
        gap: 1.5rem;
    }

    .logo img {
        height: auto;
        width: auto;
        max-height: 60px;
    }

    /* Mobile menu toggle */
    .mobile-menu-toggle {
        display: none;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5rem;
        z-index: 1001;
    }

    .hamburger {
        display: block;
        width: 28px;
        height: 2px;
        background: #fff;
        position: relative;
        transition: background 0.3s ease;
    }

    .hamburger::before,
    .hamburger::after {
        content: "";
        display: block;
        width: 28px;
        height: 2px;
        background: #fff;
        position: absolute;
        transition: transform 0.3s ease;
    }

    .hamburger::before {
        top: -8px;
    }

    .hamburger::after {
        bottom: -8px;
    }

    .mobile-menu-toggle[aria-expanded="true"] .hamburger {
        background: transparent;
    }

    .mobile-menu-toggle[aria-expanded="true"] .hamburger::before {
        transform: rotate(45deg);
        top: 0;
    }

    .mobile-menu-toggle[aria-expanded="true"] .hamburger::after {
        transform: rotate(-45deg);
        bottom: 0;
    }

    .nav-bar {
        background: #1a1a1a;
        width: 100%;
    }

    .nav-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 1rem;
        padding-bottom: 1rem;
    }

    .nav-links {
        display: flex;
        list-style: none;
        gap: 2rem;
        margin: 0;
        padding: 0;
    }

    .nav-links > li {
        position: relative;
    }

    /* Shared styles for both links and buttons */
    .nav-links > li > a,
    .nav-links > li > .nav-button {
        color: #fff;
        font-size: 1rem;
        padding: 0.5rem 0 0.75rem 0;
        display: block;
        transition: color 0.2s ease;
        text-decoration: none;

        /* Reset button styles */
        background: none;
        border: none;
        font-family: inherit;
        font-weight: inherit;
        line-height: inherit;
        text-align: left;
        cursor: pointer;
    }

    .nav-links > li > a:hover,
    .nav-links > li > a:focus,
    .nav-links > li > .nav-button:hover,
    .nav-links > li > .nav-button:focus,
    .nav-links > li:hover > a,
    .nav-links > li:hover > .nav-button,
    .nav-links > li > a.active,
    .nav-links > li > .nav-button.active {
        color: var(--primary);
        outline: none;
    }

    /* Focus visible styles for keyboard navigation */
    .nav-links > li > a:focus-visible,
    .nav-links > li > .nav-button:focus-visible {
        outline: 2px solid var(--primary);
        outline-offset: 4px;
        border-radius: 2px;
    }

    .submenu a:focus-visible {
        outline: 2px solid var(--primary);
        outline-offset: -2px;
    }

    /* Exclusive menu item */
    .nav-links.exclusive-nav {
        gap: 0;
    }

    .nav-links > li.exclusive > a,
    .nav-links > li.exclusive > .nav-button {
        background: #3a3a3a;
        border-radius: 6px;
        padding: 0.6rem 1.2rem;
        transition:
            background 0.2s ease,
            opacity 0.2s ease;
        opacity: 0.5;
    }

    .nav-links > li.exclusive:hover > a,
    .nav-links > li.exclusive > a:focus,
    .nav-links > li.exclusive:hover > .nav-button,
    .nav-links > li.exclusive > .nav-button:focus {
        background: #4a4a4a;
        opacity: 1;
    }

    /* Submenu styles */
    .submenu {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        list-style: none;
        padding: 0.5rem 0 0.75rem 0;
        margin-top: 0;
        min-width: 200px;
        z-index: 1000;
        background: #1a1a1a;
        opacity: 0;
        transform: translateY(-10px);
        transition:
            opacity 0.2s ease,
            transform 0.2s ease;
    }

    /* Add a pseudo-element to create a seamless hover bridge */
    .has-submenu::before {
        content: "";
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        height: 0.5rem;
        background: transparent;
        display: none;
    }

    .has-submenu:hover::before {
        display: block;
    }

    .has-submenu:hover .submenu,
    .has-submenu:focus-within .submenu,
    .has-submenu > .nav-button[aria-expanded="true"] + .submenu {
        display: block;
        opacity: 1;
        transform: translateY(0);
    }

    /* Position last few menu items' submenus to the right */
    .nav-links > li:nth-last-child(-n + 2) .submenu,
    .exclusive-nav .submenu {
        left: auto;
        right: 0;
    }

    .submenu li a {
        display: block;
        padding: 0.5rem 1rem;
        color: #999;
        font-size: 0.9rem;
        transition:
            color 0.2s ease,
            background 0.2s ease;
        text-align: left;
        white-space: nowrap;
    }

    .submenu li a:hover,
    .submenu li a:focus,
    .submenu li a.active {
        color: #fff;
        background: rgba(255, 255, 255, 0.05);
    }

    .language-switcher {
        display: flex;
        gap: 0.5rem;
        align-items: center;
    }

    .language-switcher a {
        font-weight: bold;
        font-size: 0.9rem;
        color: #fff;
        transition: color 0.2s ease;
        padding: 0.25rem;
    }

    .language-switcher a:hover,
    .language-switcher a:focus {
        color: var(--primary);
    }

    .language-switcher span {
        color: #666;
    }

    .language-switcher a.active {
        color: var(--primary);
    }

    /* Mobile styles */
    @media (max-width: 1024px) {
        .mobile-menu-toggle {
            display: block;
        }

        .nav-bar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: #1a1a1a;
            transform: translateX(100%);
            transition: transform 0.3s ease;
            z-index: 999;
            overflow-y: auto;
        }

        .nav-bar.is-open {
            transform: translateX(0);
        }

        .nav-container {
            flex-direction: column;
            align-items: stretch;
            padding: 6rem 2rem 2rem;
            gap: 2rem;
        }

        .nav-links {
            flex-direction: column;
            gap: 0;
        }

        .nav-links > li {
            border-bottom: 1px solid #2a2a2a;
        }

        .nav-links > li > a,
        .nav-links > li > .nav-button {
            width: 100%;
            padding: 1rem 0;
        }

        .submenu {
            position: static;
            display: none;
            opacity: 1;
            transform: none;
            margin-top: 0;
            padding: 0 0 0.5rem 1rem;
            background: transparent;
        }

        .has-submenu > .nav-button[aria-expanded="true"] + .submenu {
            display: block;
        }

        .submenu li {
            border: none;
        }

        .submenu li a {
            padding: 0.75rem 0;
        }

        .nav-links.exclusive-nav {
            border-top: 2px solid var(--primary);
            padding-top: 1rem;
        }

        .nav-links > li.exclusive > a,
        .nav-links > li.exclusive > .nav-button {
            background: transparent;
            border: 2px solid #3a3a3a;
        }

        .nav-links > li:nth-last-child(-n + 2) .submenu,
        .exclusive-nav .submenu {
            left: 0;
            right: auto;
        }
    }

    @media (max-width: 768px) {
        .header-top {
            padding: 1.5rem 0 1rem;
        }

        .logo img {
            max-height: 50px;
        }

        .language-switcher {
            gap: 0.3rem;
        }

        .language-switcher a {
            font-size: 0.8rem;
        }

        .header-right {
            gap: 1rem;
        }

        .support-btn {
            background: none;
            border: none;
            padding: 0;
        }

        .support-btn span:last-child {
            display: none;
        }

        .support-btn .heart {
            font-size: 1.2rem;
            color: #e25555;
        }
    }
</style>

<script>
    // Mobile menu toggle
    const mobileMenuToggle = document.querySelector(".mobile-menu-toggle");
    const navBar = document.querySelector(".nav-bar");
    const body = document.body;

    if (mobileMenuToggle && navBar) {
        mobileMenuToggle.addEventListener("click", () => {
            const isOpen =
                mobileMenuToggle.getAttribute("aria-expanded") === "true";
            mobileMenuToggle.setAttribute(
                "aria-expanded",
                (!isOpen).toString(),
            );
            navBar.classList.toggle("is-open");
            body.style.overflow = !isOpen ? "hidden" : "";
        });

        // Close menu when clicking outside
        document.addEventListener("click", (e) => {
            const target = e.target as HTMLElement;
            if (
                navBar.classList.contains("is-open") &&
                !navBar.contains(target) &&
                !mobileMenuToggle.contains(target)
            ) {
                mobileMenuToggle.setAttribute("aria-expanded", "false");
                navBar.classList.remove("is-open");
                body.style.overflow = "";
            }
        });

        // Close menu on escape key
        document.addEventListener("keydown", (e) => {
            if (e.key === "Escape" && navBar.classList.contains("is-open")) {
                mobileMenuToggle.setAttribute("aria-expanded", "false");
                navBar.classList.remove("is-open");
                body.style.overflow = "";
                mobileMenuToggle.focus();
            }
        });
    }

    // Handle menu buttons (non-navigational categories)
    const menuButtons = document.querySelectorAll(
        '.nav-button[aria-haspopup="true"]',
    );

    menuButtons.forEach((button) => {
        // Toggle submenu on click
        button.addEventListener("click", (e) => {
            const isExpanded = button.getAttribute("aria-expanded") === "true";

            // Close all other submenus
            menuButtons.forEach((btn) => {
                if (btn !== button) {
                    btn.setAttribute("aria-expanded", "false");
                }
            });

            // Toggle current submenu
            button.setAttribute("aria-expanded", (!isExpanded).toString());
        });

        // Enhanced keyboard navigation
        button.addEventListener("keydown", (e) => {
            const submenu = button.nextElementSibling;
            const firstLink = submenu?.querySelector("a");

            switch (e.key) {
                case "ArrowDown":
                    e.preventDefault();
                    button.setAttribute("aria-expanded", "true");
                    firstLink?.focus();
                    break;

                case "Escape":
                    button.setAttribute("aria-expanded", "false");
                    button.focus();
                    break;
            }
        });

        // Arrow key navigation within submenu
        const submenu = button.nextElementSibling;
        if (submenu) {
            const submenuLinks = submenu.querySelectorAll("a");
            submenuLinks.forEach((link, index) => {
                link.addEventListener("keydown", (e) => {
                    switch (e.key) {
                        case "ArrowDown":
                            e.preventDefault();
                            const nextLink =
                                submenuLinks[index + 1] || submenuLinks[0];
                            nextLink.focus();
                            break;

                        case "ArrowUp":
                            e.preventDefault();
                            if (index === 0) {
                                button.focus();
                                button.setAttribute("aria-expanded", "false");
                            } else {
                                submenuLinks[index - 1].focus();
                            }
                            break;

                        case "Escape":
                            e.preventDefault();
                            button.setAttribute("aria-expanded", "false");
                            button.focus();
                            break;
                    }
                });
            });
        }
    });

    // Close submenus when clicking outside
    document.addEventListener("click", (e) => {
        const target = e.target as HTMLElement;
        if (!target.closest(".has-submenu")) {
            menuButtons.forEach((button) => {
                button.setAttribute("aria-expanded", "false");
            });
        }
    });

    // Allow closing submenu when tabbing away
    const allMenuLinks = document.querySelectorAll(".nav-links a");
    allMenuLinks.forEach((link) => {
        link.addEventListener("blur", () => {
            setTimeout(() => {
                const parentItem = (link as HTMLElement).closest(
                    ".has-submenu",
                );
                const submenu = parentItem?.querySelector(".submenu");

                if (
                    submenu &&
                    !submenu.contains(document.activeElement) &&
                    !parentItem?.contains(document.activeElement)
                ) {
                    const parentButton = parentItem?.querySelector(
                        ".nav-button[aria-haspopup]",
                    );
                    if (parentButton) {
                        parentButton.setAttribute("aria-expanded", "false");
                    }
                }
            }, 100);
        });
    });
</script>
